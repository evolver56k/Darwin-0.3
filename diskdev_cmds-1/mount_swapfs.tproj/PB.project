{
    APPCLASS = NSApplication; 
    FILESTABLE = {
	FRAMEWORKS = (); 
	M_FILES = (); 
	OTHER_LIBS = (); 
	OTHER_LINKED = (mount_swapfs.c); 
	OTHER_SOURCES = (Makefile.preamble, Makefile, Makefile.postamble); 
    }; 
    INSTALLDIR = /sbin; 
    LANGUAGE = English; 
    LOCALIZABLE_FILES = {}; 
    MAINNIB = mount_swapfs; 
    PROJECTNAME = mount_swapfs; 
    PROJECTTYPE = Tool; 
    PROJECTVERSION = 2.0; 
}

{
    APPCLASS = NSApplication; 
    COMPILEROPTIONS = "-DMACH_USER_API"; 
    FILESTABLE = {
	FRAMEWORKS = (); 
	HEADERSEARCH = ("${MOUNT}"); 
	M_FILES = (); 
	OTHER_LIBS = (); 
	OTHER_LINKED = (mount_kernfs.c); 
	OTHER_SOURCES = (Makefile.preamble, Makefile, Makefile.dist, mount_kernfs.8); 
    }; 
    INSTALLDIR = /sbin; 
    LANGUAGE = English; 
    LOCALIZABLE_FILES = {}; 
    MAINNIB = mount_kernfs; 
    PROJECTNAME = mount_kernfs; 
    PROJECTTYPE = Tool; 
    PROJECTVERSION = 2.0; 
}

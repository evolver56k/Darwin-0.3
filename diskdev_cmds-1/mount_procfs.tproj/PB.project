{
    APPCLASS = NSApplication; 
    COMPILEROPTIONS = "-DMACH_USER_API"; 
    FILESTABLE = {
	FRAMEWORKS = (); 
	HEADERSEARCH = ("${MOUNT}"); 
	M_FILES = (); 
	OTHER_LIBS = (); 
	OTHER_LINKED = (mount_procfs.c); 
	OTHER_SOURCES = (Makefile.preamble, Makefile, Makefile.dist, mount_procfs.8); 
    }; 
    INSTALLDIR = /sbin; 
    LANGUAGE = English; 
    LOCALIZABLE_FILES = {}; 
    MAINNIB = mount_procfs; 
    PROJECTNAME = mount_procfs; 
    PROJECTTYPE = Tool; 
    PROJECTVERSION = 2.0; 
}

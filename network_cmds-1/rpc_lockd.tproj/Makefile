#
# Generated by the NeXT Project Builder.
#
# NOTE: Do NOT change this file -- Project Builder maintains it.
#
# Put all of your customizations in files called Makefile.preamble
# and Makefile.postamble (both optional), and Makefile will include them.
#

NAME = rpc.lockd

PROJECTVERSION = 2.6
PROJECT_TYPE = Tool
LANGUAGE = English

HFILES = klm_prot.h lockf.h lockmgr.h nlm_prot.h priv_prot.h\
         prot_lock.h prot_time.h sm_inter.h sm_res.h

CFILES = hash.c prot_alloc.c prot_freeall.c prot_libr.c prot_lock.c\
         prot_main.c prot_msg.c prot_pklm.c prot_pnlm.c prot_priv.c\
         prot_proc.c prot_share.c sm_monitor.c svc_udp.c tcp.c udp.c\
         ufs_lockf.c xdr_klm.c xdr_nlm.c xdr_sm.c

OTHERSRCS = Makefile.dist Makefile.preamble


MAKEFILEDIR = $(MAKEFILEPATH)/pb_makefiles
CODE_GEN_STYLE = DYNAMIC
MAKEFILE = tool.make
NEXTSTEP_INSTALLDIR = /usr/libexec
WINDOWS_INSTALLDIR = /usr/libexec
PDO_UNIX_INSTALLDIR = /usr/libexec
LIBS = 
DEBUG_LIBS = $(LIBS)
PROF_LIBS = $(LIBS)


NEXTSTEP_PB_CFLAGS = -DVAX_PORT
WINDOWS_PB_CFLAGS = -DVAX_PORT
PDO_UNIX_PB_CFLAGS = -DVAX_PORT


include $(MAKEFILEDIR)/platform.make

-include Makefile.preamble

include $(MAKEFILEDIR)/$(MAKEFILE)

-include Makefile.postamble

-include Makefile.dependencies

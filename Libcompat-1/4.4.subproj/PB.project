{
    FILESTABLE = {
        C_FILES = (); 
        H_FILES = (); 
        OTHER_LINKED = (cuserid.c); 
        OTHER_SOURCES = (Makefile.preamble, Makefile, Makefile.postamble); 
    }; 
    GENERATEMAIN = YES; 
    LANGUAGE = English; 
    LOCALIZABLE_FILES = {}; 
    NEXTSTEP_BUILDTOOL = /bin/gnumake; 
    NEXTSTEP_DOCUMENTEXTENSIONS = (); 
    NEXTSTEP_INSTALLDIR = /usr/local/lib; 
    PROJECTNAME = 4.4; 
    PROJECTTYPE = Subproject; 
    PROJECTVERSION = 2.6; 
}

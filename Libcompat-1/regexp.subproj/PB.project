{
    FILESTABLE = {
        C_FILES = (); 
        H_FILES = (regexp.h, regmagic.h); 
        OTHER_LINKED = (regerror.c, regex.c, regexp.c, regsub.c); 
        OTHER_SOURCES = (Makefile.preamble, Makefile, Makefile.postamble); 
        PUBLIC_HEADERS = (); 
    }; 
    LANGUAGE = English; 
    LOCALIZABLE_FILES = {}; 
    NEXTSTEP_BUILDTOOL = /bin/gnumake; 
    NEXTSTEP_INSTALLDIR = /usr/local/lib; 
    PROJECTNAME = regexp; 
    PROJECTTYPE = Subproject; 
    PROJECTVERSION = 2.6; 
}

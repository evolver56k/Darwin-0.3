{
    FILESTABLE = {
        Documentation = (); 
        Headers = (); 
        Makefiles = (passwdawk.in, groupawk.in); 
        Resources = (); 
        SUBPROJECTS = (); 
        Sources = (grcat.c, pwcat.c); 
    }; 
    NEXTSTEP_BUILDTOOL = /bin/make; 
    PDO_UNIX_BUILDTOOL = /bin/make; 
    PROJECTNAME = lib; 
    PROJECTTYPE = "GNU Source"; 
    PROJECTVERSION = 2.6; 
    WINDOWS_BUILDTOOL = "${NEXT_ROOT}/NextDeveloper/Executables/make"; 
}

現在マウントされていないハードディスクパーティション上のファイルシステ
ムからのインストール。

そのパーティションには、インストールされる各 distribution area（通常は 
stable, contrib, non-free）の binary/*/*.deb ファイルに一致するような 
Packages.gz ファイルが含まれている必要があります。

この為の最も単純な方法は、（可能であれば）distribution サイトのディレ
クトリ階層の（部分的な）コピーを作ることです。

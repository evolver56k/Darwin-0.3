#
# Generated by the NeXT Project Builder.
#
# NOTE: Do NOT change this file -- Project Builder maintains it.
#
# Put all of your customizations in files called Makefile.preamble
# and Makefile.postamble (both optional), and Makefile will include them.
#

NAME = libedit

PROJECTVERSION = 2.6
PROJECT_TYPE = Library
LANGUAGE = English

HFILES = chared.h common.h el.h emacs.h fcns.h help.h hist.h\
         histedit.h key.h map.h parse.h prompt.h refresh.h search.h\
         sig.h sys.h term.h termcap.h tokenizer.h tty.h vi.h

CFILES = editline.c history.c tokenizer.c

OTHERSRCS = Makefile.preamble Makefile Makefile.postamble README.NeXT\
            Makefile.dist makelist chared.c common.c el.c emacs.c\
            hist.c key.c map.c parse.c prompt.c read.c refresh.c\
            search.c sig.c term.c tty.c vi.c fcns.c help.c


MAKEFILEDIR = $(MAKEFILEPATH)/pb_makefiles
CURRENTLY_ACTIVE_VERSION = YES
DEPLOY_WITH_VERSION_NAME = A
CODE_GEN_STYLE = DYNAMIC
MAKEFILE = library.make
NEXTSTEP_INSTALLDIR = /usr/local/lib
WINDOWS_INSTALLDIR = /usr/local/lib
PDO_UNIX_INSTALLDIR = /usr/local/lib
LIBS = 
DEBUG_LIBS = $(LIBS)
PROF_LIBS = $(LIBS)


NEXTSTEP_PB_CFLAGS = -fno-builtin
WINDOWS_PB_CFLAGS = -fno-builtin -nostdinc
PDO_UNIX_PB_CFLAGS = -fno-builtin -nostdinc
PUBLIC_HEADERS = histedit.h



include $(MAKEFILEDIR)/platform.make

-include Makefile.preamble

include $(MAKEFILEDIR)/$(MAKEFILE)

-include Makefile.postamble

-include Makefile.dependencies
